apiVersion: v1
kind: Service
metadata:
  name: license-management-system-vendor-service
spec:
  selector:
    app: license-management-system-vendor-label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
